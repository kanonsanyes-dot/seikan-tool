# FIX_REPORT_20260513_FIX12_CODEX_CLEANUP_READY

## 目的

Codex投入前に、SeikanToolの状態を整理した最終確認メモです。

## Fix12で確認・修正した内容

- スケジューラー未定義工程のfallback色をデザインシステム準拠に修正
  - `#D3D1C7` / `#5F5E5A` / `#444441`
- ダッシュボードChart.jsのtick/grid色をウォームアンバー系に修正
  - text: `rgba(99, 56, 6, 0.7)`
  - grid: `rgba(240, 217, 184, 0.6)`
- ダークモード分岐は工場PCライトモード前提のため削除
- `.gitignore` を追加し、以下を除外対象にする方針
  - `.venv/`
  - `__pycache__/`
  - `*.pyc`
  - `*.db`
  - `data/*.db`
  - `data/logs/`
  - `data/temp/`
  - `uploads/`

## Codexへの依頼方針

初回Codex作業では、大改造はしないでください。

優先するのは以下です。

1. 起動不能の修正
2. 500エラーの修正
3. テンプレートエラーの修正
4. `.gitignore` の不足確認
5. CSS重複・副作用の報告
6. スケジューラー操作系の重大バグ確認

CSS全面整理、DB設計変更、画面構成変更、ライブラリ追加は初回では行わないでください。
